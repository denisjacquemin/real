<% content_for :sidebar do %>
  <%= render 'admin/themes/sidebar' %>
<% end %>
<% content_for :breadcrumbs do %>
  <ul class="breadcrumb">
    <li><a href="#">Themes</a> <span class="divider">/</span></li>
    <li class="active">Editing <%= @template.title %></li>
  </ul>
<% end %>
<div class="block">
  <h1>Editing Template <%= @template.title %></h1>
  <%= render 'form' %>
</div>