<%= form_for(@admin_template) do |f| %>
  <% if @admin_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_template.errors.count, "error") %> prohibited this admin_template from being saved:</h2>

      <ul>
      <% @admin_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
